@using DiveO.Services
@model DiveO.ViewModels.DiveViewModel

        <div class="container">
            <div class="row">
                <div class="well col-sm-8 col-sm-offset-2">
                    <div class="content">
                        @{
                            var img = new ImageProcessor().ByteToImage(Model.Diver.ProfilePic);
                        }
                        <img src="@img" class="img-circle" width="60" height="60" style="display: inline-block" />
                        <h3 style="display: inline-block">@Html.DisplayFor(model => model.Diver.Name)</h3>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="well col-sm-8 col-sm-offset-2">
                        @foreach (var item in Model.DiveList)
                        { 
                            <div class="content">

                                <p>@Html.ActionLink(@item.DiveSite, "Details", "Dive", new {id = item.Id }, null)</p>

                                @*<p>@Html.DisplayFor(i => item.DiveSite)</p>
                                <p style="display: inline-block">@Html.DisplayFor(i => item.Location)</p>*@    
                            </div>
                        }
                    <button class="btn btn-primary">@Html.ActionLink("Add A Dive", "Create", "Dive", new { id = Model.Diver.Id }, null)</button>
                </div>
            </div>
        </div>  